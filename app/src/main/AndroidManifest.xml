<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConnectEvent">

        <!-- LAUNCHER -->
        <activity
            android:name=".view.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- AUTH -->
        <activity android:name=".view.LoginPage" />
        <activity android:name=".view.RegisterPage" />
        <activity android:name=".view.ForgetPasswordPage" />

        <!-- DASHBOARD -->
        <activity android:name=".view.DashboardPage" />

        <!-- EVENTS -->
        <activity android:name=".view.CreateEventPage" />
        <activity android:name=".ViewEventPage" />
        <activity android:name=".view.MyEventsPage" />

    </application>

</manifest>
