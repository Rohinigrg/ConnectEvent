<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConnectEvent">

        <!-- LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- AUTH -->
        <activity android:name=".LoginPage" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".ForgetPasswordPage" />

        <!-- DASHBOARD -->
        <activity android:name=".DashboardPage" />

        <!-- EVENTS -->
        <activity android:name=".CreateEventPage" />
        <activity android:name=".ViewEventPage" />
        <activity android:name=".MyEventsPage" />

    </application>

</manifest>
